<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <hr>
        <small>Data de puiblicação: {{postagem.data}}</small>
        <hr>
        {{postagem.conteudo}}
    </div>
</div>